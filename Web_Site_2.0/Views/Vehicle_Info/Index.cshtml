@model IEnumerable<Web_Site_2._0.Models.Vehicle_Info>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutThird.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Van_Licence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Van_Licence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Capacity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Van_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Van_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Van_Id })
        </td>
    </tr>
}

</table>
